apply from: rootProject.file('gradle/module-custom-check.gradle')

lintRegistry('net.twisterrob.lints.lint.LintApiUnspecifiedRegistry')

// REPORT ObsoleteLintCustomCheck doesn't report "which did not specify the Lint API version it was compiled with" any more.
expectedViolationCount(0)
