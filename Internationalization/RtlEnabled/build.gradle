apply from: rootProject.file('gradle/module-check-lib.gradle')

// REPORT android:paddingStart is not considered an RTL attribute (mUsesRtlAttributes)

// REPORT RtlEnable doesn't trigger any more on this project, tried for more than an hour but no luck.
expectedViolationCount(0)
